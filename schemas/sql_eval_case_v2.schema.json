{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sql_eval_case_v2.schema.json",
  "title": "SQL Eval Case v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "case_id", "source_benchmark", "source_split", "task_id", "db_id", "db_path", "dialect", "question", "schema_text", "gold_sql", "task_type", "verification", "order_sensitive", "numeric_tolerance"],
  "properties": {
    "schema_version": {"const": "sql_eval_case:v2"},
    "case_id": {"type": "string", "minLength": 1},
    "source_benchmark": {"const": "livesqlbench"},
    "source_split": {"enum": ["dev", "eval", "challenge"]},
    "task_id": {"type": "string", "minLength": 1},
    "db_id": {"type": "string", "minLength": 1},
    "db_path": {"type": "string", "minLength": 1},
    "dialect": {"enum": ["postgresql", "sqlite"]},
    "question": {"type": "string", "minLength": 1},
    "schema_text": {"type": "string", "minLength": 1},
    "knowledge_text": {"type": ["string", "null"]},
    "gold_sql": {"type": "string", "minLength": 1},
    "task_type": {"enum": ["select", "management"]},
    "verification": {"$ref": "#/$defs/verification"},
    "order_sensitive": {"type": "boolean"},
    "numeric_tolerance": {"type": "number", "minimum": 0}
  },
  "$defs": {
    "verification": {
      "type": "object", "additionalProperties": false,
      "required": ["status", "verified_by", "verification_id", "verified_at"],
      "properties": {
        "status": {"const": "execution_verified"},
        "verified_by": {"type": "string", "minLength": 1},
        "verification_id": {"type": "string", "minLength": 1},
        "verified_at": {"type": "string", "minLength": 1}
      }
    }
  }
}
